- name: variables from prompt
  hosts: local
  connection: local
  vars_prompt:
  - name: USERNAME 
    prompt: please enter youe username
    private: true

  - name: PASSWORD
    prompt: please enter your password
    private: true
  tasks:
  - name: printing user infromation
    ansible.builtin.debug:
      msg: "Hello Username is: {{ USERNAME }}, password is: {{ PASSWORD }}"

